name: Deploy to local Minikube

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on:
      - self-hosted
      - laptop-horo

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test if runner works
        run: |
          echo "✅ Running on laptop-horo"
          hostname
