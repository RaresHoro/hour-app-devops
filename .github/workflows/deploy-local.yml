name: Test Self-Hosted Runner

on:
  push:
    branches:
      - dev  # or whichever branch you're pushing to

jobs:
  test-runner:
    runs-on: self-hosted

      

    steps:
      - name: Print runner info
        run: |
          echo "✅ This is running on a self-hosted runner!"
          echo "Hostname: $(hostname)"
